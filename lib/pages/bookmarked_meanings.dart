import 'package:flutter/material.dart';

class BookmarkedMeanings extends StatelessWidget {
  const BookmarkedMeanings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
